{
  "name": "sple-exam-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "npm run build && netlify deploy --prod",
    "preview": "npm run build && netlify deploy",
    "analyze": "cross-env ANALYZE=true npm run build",
    "type-check": "tsc --noEmit",
    "test": "echo \"No tests specified\" && exit 0",
    "check-deployment": "node scripts/check-deployment.js",
    "prebuild": "npm run check-deployment"
  },
  "dependencies": {
    "autoprefixer": "^10.4.16",
    "firebase": "^10.12.2",
    "lucide-react": "^0.400.0",
    "next": "14.2.5",
    "postcss": "^8.4.32",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "cross-env": "^7.0.3",
    "eslint": "^8",
    "eslint-config-next": "14.2.5",
    "netlify-cli": "^17.0.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.17",
    "typescript": "^5"
  }
}
